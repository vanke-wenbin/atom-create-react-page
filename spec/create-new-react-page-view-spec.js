'use babel';

import CreateNewReactPageView from '../lib/create-new-react-page-view';

describe('CreateNewReactPageView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
